import React from "react";

const Loading = () => {
  return (
    <div className="loading">
      <i className="fas fa-spinner fas-spin">Loading...</i>
    </div>
  );
};

export default Loading;
